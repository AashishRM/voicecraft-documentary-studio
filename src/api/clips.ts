import { GetRequest, PostRequest } from "../plugins/https"

// export const APIGetAllMessages = ()=>{
//     return GetRequest('message');
// }
// export const APISendMessage = (conversationId: string, body: { text: string }) => {
//   return PostRequest(`message`, body);
// };
// export const APIGetMessages = (conversationId: string) => {
//   return GetRequest(`message/${conversationId}/messages`);
// };


// export const APISendPrompt = (body: { text: string }) => {
//   return PostRequest("prompt", body);
// };